#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
      for(int i=0;i<n;i++)
      {
        scanf("%d",&a[i]);
      }
      int *p=a;
     
      for(int i=0;i<n;i++)
      {
          for(int j=0;j<n-i-1;j++)
          {
              if(*(p+j)>*(p+j+1))
              {
                  int x=*(p+j);
                  *(p+j)=*(p+j+1);
                  *(p+j+1)=x;
              }
          }
      }
      
      for(int i=0;i<n;i++)
      {
        printf("%d ",*(p+i));
          
      }
    
    return 0;
}
