#include<stdio.h>
void swap(int *l,int *r)
{
    int x=*l;
    *l=*r;
    *r=x;
}
void permute(int a[],int l,int r,int n)
{
    if(l==r)
    {
        for(int i=0;i<n;i++)
        {
            printf("%d",a[i]);
        }
        printf("\n");
        return;
    }
    for(int i=l;i<=r;i++)
    {
        swap(&a[l],&a[i]);
        permute(a,l+1,r,n);
        swap(&a[l],&a[i]);
        
    }
}
int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    permute(a,0,n-1,n);
}
