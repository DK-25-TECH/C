#include<stdio.h>
#include<stdbool.h>
int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    int b[n];
    int z=0;
    for(int i=0;i<n-1;i++)
    {
        bool x=true;
        for(int j=i+1;j<n;j++)
        {
            if(a[j]>=a[i])
            {
                x=false;
                break;
            }
           
        }
        if(x)
        {
            b[z++]=a[i];
        }
    }
    b[z++]=a[n-1];
    for(int i=0;i<z;i++)
    {
        printf("%d ",b[i]);
    }
    
}
