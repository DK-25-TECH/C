#include<stdio.h>
#include<string.h>
#include<ctype.h>

int fibb(int n)
{
    if(n<=1)
    {
        return n;
    }
    return fibb(n-1)+fibb(n-2);
}
int main()
{
   int n;
   scanf("%d",&n);
   int x=fibb(n);
   printf("%d",x);
}
