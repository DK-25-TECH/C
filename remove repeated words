
#include <stdio.h>
#include<string.h>

int main()
{
    char c[200];
    char *p[200];
    fgets(c,sizeof(c),stdin);
    int n=strlen(c);
    if(c[n-1]=='\n')
    {
        c[n-1]='\0';
        n--;
    }
    char *x=strtok(c," ");
    int j=0;
    while(x!=NULL)
    {
        int f=1;
        for(int i=0;i<j;i++)
        {
            if(strcmp(p[i],x)==0)
            {
                f=0;
                break;
            }
        }
        if(f)
        {
            p[j++]=x;
        }
        x=strtok(NULL," ");
    }
    for(int i=0;i<j;i++)
    {
        printf("%s ",p[i]);
    }
    
}
