
#include <stdio.h>
#include<string.h>
#include<ctype.h>
int main()
{
   char c[100];
   char *x[100];
   fgets(c,sizeof(c),stdin);
   int n=strlen(c)-1;
   int z=0;
   if(c[n]=='\n')
   {
       c[n]='\0';
           n--;
   }
   char *p=strtok(c," ");
   while(p!=NULL)
   {
       x[z++]=p;
       p=strtok(NULL," ");
   }
   for(int i=0;i<z-1;i++)
   {
       for(int j=i+1;j<z;j++)
       {
           if(strcmp(x[i],x[j])>0)
           {
               char *q=x[i];
               x[i]=x[j];
               x[j]=q;
           }
       }
   }
   for(int i=0;i<z;i++)
       printf("%s\n",x[i]);
   
    return 0;
}
