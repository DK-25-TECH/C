#include <stdio.h>
#include<string.h>
#include<ctype.h>
#include<stdlib.h>
char* rnc(char *a,int n1)
{
    char *p=malloc(n1*sizeof(char));
    char *x=p;
    while(*a)
    {
        if(!isdigit(*a))
        {
           *p=*a; 
           p++;
        }
        a++;
    }
    *p='\0';
    
    return x;
}
int main()
{
    char a[100];
    fgets(a,sizeof(a),stdin);
    int n1=strlen(a);
    if(a[n1-1]=='\n')
    {
        a[n1-1]='\0';
        n1--;
    }
    char *p=rnc(a,n1);
    printf("%s",p);
}
