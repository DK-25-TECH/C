#include<stdio.h>
#include<string.h>
int main() {
    char c[100];
    fgets(c,sizeof(c),stdin);
    int n=strlen(c);
    if(c[n-1]=='\n')
    {
        c[n-1]='\0';
    }
    char *p=c;
    int x=0;
    while(*p)
    {
        x++;
        p++;
    }
    printf("%d",x);
}
