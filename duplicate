#include<stdio.h>
#include<limits.h>
int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    int mx=a[0];
    for(int i=0;i<n;i++)
    {
       if(a[i]>mx)
       {
           mx=a[i];
       }
    }
    int fq[mx+1];
    for(int i=0;i<=mx;i++)
    {
        fq[i]=0;
    }
    for(int i=0;i<n;i++)
    {
        fq[a[i]]++;
    }
    for(int i=0;i<n;i++)
    {
        if(fq[a[i]]==1)
        {
            printf("%d",a[i]);
        }
    }
}
