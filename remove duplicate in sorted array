#include<stdio.h>
#include<string.h>
#include<ctype.h>

int rem(int *a,int n)
{
   int x=0;
   for(int i=0;i<n-1;i++)
   {
       if(a[i]!=a[i+1])
       {
           a[x++]=a[i];
       }
   }
   a[x++]=a[n-1];
   return x;
}
int main()
{
   int n;
   scanf("%d",&n);
   int a[n];
   for(int i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
   }
   int z=rem(a,n);
   for(int i=0;i<z;i++)
   printf("%d ",a[i]);
}
