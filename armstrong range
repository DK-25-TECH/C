#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int amstrong(int n)
{
    
    int tp=n;
    for(int i=0;i<n;i++)
    {
        int x=0,c=0,z=i;;
        while(z>0)
        {
            c++;
            z/=10;
        }
        z=i;
    while(z>0)
    {
        int rem=z%10;
        x+=pow(rem,c);
        z/=10;
    }
    if(x==i)
    {
       printf("%d ",x);
    }
    }
   
}
    
int main()
{
    int n;
    scanf("%d",&n);
    amstrong(n);
   
}
