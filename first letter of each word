#include <stdio.h>
#include<string.h>
#include<ctype.h>
#include<stdlib.h>

int main()
{
   char c[100];
   fgets(c,sizeof(c),stdin);
   int n=strlen(c)-1;
   if(c[n]=='\n')
   {
       c[n]='\0';
       n--;
   }
  int x=0;
   for(int i=0;i<n;i++)
   {
       if(!isspace(c[i]) && !x)
       {
           printf("%c",c[i]);
           x=1;
       }
       else if(isspace(c[i]))
       {
           x=0;
       }
   }
   
}
