#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    char c[100];
    fgets(c,sizeof(c),stdin);
    int n=strlen(c);
    if(c[n-1]=='\n')
    {
        c[n-1]='\0';
        n--;
    }
    
    int j=0;
    
    int x[256]={0};
    
    for(int i=0;i<n;i++)
    {
        x[(unsigned char)c[i]]++;
    }
    
    for(int i=0;i<n;i++)
    {
        if(x[(unsigned char)c[i]]==1)
        {
            c[j++]=c[i];
        }
    }
    for(int i=0;i<j;i++)
    {
        printf("%c ",c[i]);
    }
}
