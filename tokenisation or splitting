#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
    char a[100];
    fgets(a,sizeof(a),stdin);
    int n=strlen(a);
    if(a[n-1]=='\n')
    {
        a[n-1]='\0';
       
    }
    
    char *x=a;
    x=strtok(a," ");
    while(x==NULL)
    {
        printf("%s\n",x);
        x=strtok(NULL," ");
    }
}
