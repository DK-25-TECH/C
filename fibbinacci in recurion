#include<stdio.h>
#include<string.h>
int fibbi(int n)
{
    if(n<=1)
    {
        return n;
    }
    return fibbi(n-1)+fibbi(n-2);
}
int main()
{
    int n;
    scanf("%d",&n);
    int x=fibbi(n);
    printf("%d ",x);
}
