#include<stdio.h>

void f(int i,int fact,int x)
{
    if(i>x)
    {
        printf("%d",fact);
        return;
    }
    f(i+1,fact*i,x);
}
int main()
{
    int x,i=1,fact=1;;
    scanf("%d",&x);
    
    f(i,fact,x);
    return 0;
}
