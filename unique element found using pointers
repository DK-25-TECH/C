#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    char c[100];
    fgets(c,sizeof(c),stdin);
    int n=strlen(c);
    if(*(c+n-1)=='\n')
    {
        *(c+n-1)='\0';
        n--;
    }
    char *j=c;
    
    int x[256]={0};
    
    for(int i=0;i<n;i++)
    {
        x[(unsigned char)*(j+i)]++;
    }
    
    char *z=c;
    int l=0;
    for(int i=0;i<n;i++)
    {
        if(x[(unsigned char)*(j+i)]==1)
        {
            *(z+l)=*(c+i);
            l++;
        }
    }
    for(int i=0;i<l;i++)
    {
        printf("%c ",*(c+i));
    }
}
