#include<stdio.h>

void findcom(int i,int a[],int b[],int x,int sum,int n,int blen)
{
    if(sum==x)
    {
            printf("{");
        for(int i=0;i<blen;i++)
        {
            printf("%d",b[i]);
            if(i<blen-1)
            {
                printf(",");
            }
        }
            printf("}");
            return;
    }
    if(i==n ||sum>x) return;
    b[blen]=a[i];
    findcom(i+1,a,b,x,sum+a[i],n,blen+1);
    findcom(i+1,a,b,x,sum,n,blen);
}
int main()
{
    int n;
    scanf("%d",&n);
    int sum=0;
    int a[n];
    int b[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    int x;
    scanf("%d",&x);
    findcom(0,a,b,x,sum,n,0);
}
